{
  "buildCommand": "npm run build && mkdir -p dist/api && cp api/claude.js dist/api/",
  "outputDirectory": "dist",
  "functions": {
    "api/claude.js": {
      "memory": 256,
      "maxDuration": 30
    }
  }
}
